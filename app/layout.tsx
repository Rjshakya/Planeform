import type { Metadata } from "next";
import {
  Geist_Mono,
  DM_Sans,
  Inter,
  Geist,
  Space_Grotesk,
  Playfair_Display,
  Roboto,
  Roboto_Mono,
  Roboto_Serif,
  Poppins,
  Acme,
  DM_Mono,
} from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";
import { cn } from "@/lib/utils";

// const geistSans = Geist({
//   variable: "--font-geist-sans",
//   subsets: ["latin"],
// });

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const dmSans = DM_Sans({
  variable: "--font-dm-sans",
  subsets: ["latin"],
});

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const geist = Geist({
  variable: "--font-geist",
  subsets: ["latin"],
});

const spaceGrotesk = Space_Grotesk({
  variable: "--font-space-grotesk",
  subsets: ["latin"],
});

const playFair = Playfair_Display({
  variable: "--font-playfair-display",
  subsets: ["latin"],
});

const roboto = Roboto({
  variable: "--font-roboto",
  subsets: ["latin"],
});
const robotoMono = Roboto_Mono({
  variable: "--font-roboto-mono",
  subsets: ["latin"],
});

const robotoSerif = Roboto_Serif({
  variable: "--font-roboto-serif",
  subsets: ["latin"],
});

const poppins = Poppins({
  variable: "--font-poppins",
  subsets: ["latin"],
  weight: "400",
});

const acme = Acme({
  variable: "--font-acme",
  subsets: ["latin"],
  weight: "400",
});

export const metadata: Metadata = {
  title: "Planetform",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={cn(`${dmSans.variable} ${geistMono.variable} ${inter.variable} ${geist.variable} ${spaceGrotesk.variable} ${playFair.variable} antialiased , ${roboto.variable}
          ${robotoMono.variable} ${robotoSerif.variable} ${poppins.variable} ${acme.variable}`)}
      >
        <div
          // max-w-6xl mx-auto
          className={`${spaceGrotesk.className}  tracking-tighter w-full`}
        >
          {children}
        </div>
        <Toaster position="top-right" />
      </body>
    </html>
  );
}
